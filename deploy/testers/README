Generated by ../new_tester.sh, one set per tester, removed by ../remove_tester.sh:

  NAME.yml    the compose service (extends ../tester.yml)
  NAME.env    PORT, ACCESS_TOKEN, ROOT_PATH -- the token is a secret

The matching route is ../routes/NAME.caddy; only routes/ is mounted into
the Caddy container, so the tokens here never reach the proxy.
