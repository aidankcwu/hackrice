# Ralph progress (demo, native)
Started: Thu Sep 24 20:14:50 CDT 2026
---

## 2026-09-24 D-001 fixed header and three tabs (passes)
What I did
- Tabs: Home (house), Analysis, Protocol. Calendar tab gone. AppScreen ids: home, analysis,
  protocol, connect, settings, preview; `-screen today` still maps to home.
- Header, shared by every tab through the `AppHeader` ViewModifier in Screens/StatusPill.swift:
  navigation bar = StatusPill (leading: battery %, eyeglasses ink/muted, dot + status) and a
  trailing group [Preview camera.viewfinder][gear]; Start watching / Stop (WatchButton,
  .glassProminent, full width, .large) sits in `.safeAreaBar(edge: .top)` under the bar.
  RootView owns the sheets (Connect, Settings, Preview, consent gate).
- HeaderState.derive (App/HeaderState.swift) is pure; Tests/HeaderStateTests.swift.
- GlassesSessioning gained `deviceState: GlassesDeviceState?` + `onDeviceStateChange`.
  GlassesSession: deviceForIdentifier(first device) + Device.addDeviceStateListener, nil
  unless snapshot.linkState == .connected. Mock/Demo glasses: `.demo` (82%, worn).
  AppState.glassesDevice; -glassesOff and -fresh set it nil. AppState.headerState(now:).
- TodayView -> HomeView (Screens/HomeView.swift), its own Start button removed; content as before.
- PreviewView is a placeholder sheet ("Glasses view", "No frames yet") for D-002.
- Protocol: Add moved out of the toolbar into its own clear Section above the list (D-005 reworks).

Decisions (the orchestrator should look at these)
- DAT SDK bumped 0.9.0 -> 1.0.0 (project.yml `from: "1.0.0"`, own commit 661b644). 0.9.x has no
  battery/worn at all; 1.0.0 (tagged 2026-09-24) adds Device.batteryLevel/donState and
  addDeviceStateListener. The app built unchanged. UNTESTED ON HARDWARE; revert that commit
  plus the GlassesSession observeDeviceState block if the glasses misbehave.
- All four header controls in one navigation bar do NOT fit a 402 pt iPhone: the system folded
  Preview and the gear into "•••" even with "Stop" and a 13 pt pill. Hence the two-row header
  (nav bar + safeAreaBar). Both are system bars and stay fixed while the page scrolls.
- Tabs use `.toolbarTitleDisplayMode(.inline)` (set in AppHeader): a large title sat under the
  Start/Stop bar's scroll-edge fade and read greyed out. The inline title gets no room next to
  the pill, so the tab bar is what names the tab. D-005's "3 of 5 today" row can carry Protocol.
- Pill and the two icon buttons are capped at DynamicTypeSize.large with the Large Content
  Viewer, like system bar items; otherwise the gear folds into "•••" at accessibility sizes.

Next iteration must know
- Build/test: xcodebuild's exit code gets lost behind `| grep`; redirect to a log and check $?.
- `git -C /Users/ljawhomebot/hackrice ...` instead of `cd … && git` (the permission hook asks).
- Screenshots: sips -Z 1000 a copy into the scratchpad before reading; the full PNG is fine too.

## 2026-09-24 D-003 Home metrics, tiles, watched line, hero sheet (passes)
What I did
- App/TodayStats.swift (pure): TodayStats.minutes(kind:episodes:now:) + durationText ("14 min",
  "2 h 05 min"); HomeMetrics.derive(episodes:sessions:watchingSince:now:calendar:) -> daylight,
  screens, watchedLine; HealthFactorText (provenance word, dose with unit, citation, order).
  Tests/TodayStatsTests.swift covers all of it, fixture included (Daylight 10 min, Screens 28 min).
- Models: Healthspan.factors ([HealthFactor], decoded leniently via Lenient<T>), Episode.durationS
  (var, default nil so the memberwise init still works), WatchSession {id, name, startedT, endedT}
  for GET /api/sessions. AppState.sessions exists but is EMPTY until D-006 fetches it; the watched
  line then switches from the watching span to the sessions sum by itself.
- HomeView: hero panel is a plain Button (info.circle top-right, muted) -> MeasuredView sheet;
  two Brian.surface tiles (radius 20, HStack, VStack at accessibility sizes); watched line. The old
  "Score 76 · −0.1 years" line is gone (not in the PRD). Ledger unchanged (D-008).
- MeasuredView (Screens/): List insetGrouped, the PRD paragraph, then factors biggest |hours| first,
  missing last: label + SignedHoursChip, "5,500 steps · Seeded", citation (source up to its ": ").
- AppScreen gained `measured` (-screen measured opens Home with the sheet up). RootView owns it.

Decisions
- /api/healthspan factors carry no unit; HealthFactorText.unit(for:) is a plain-word table keyed by
  brian_score.py FACTORS keys (falls back to a decoded `unit` if the backend ever sends one).
- Provenance words: Glasses = live/derived with basis glasses; Missing = missing/unmeasured/no dose;
  everything else Seeded (so derived-from-apple_watch reads Seeded, which is true in the demo).
- An open episode's length is the server's duration_s, not now − start: the fixture's open
  outdoor_block is days old and read "52 h 34 min" of daylight before this.

Next iteration must know
- `-screen measured` is the hero-sheet screenshot id (D-010 needs it).
- HomeView takes `openMeasured` from RootView; keep new sheets in RootView too.

## 2026-09-24 D-004 Home daily summary card (passes)
What I did
- Models: Recap {id, generatedAt, headline, paragraphs, suggestions}; decodes the wire's nested
  `narrative` (POST /api/recap, GET /api/recaps/{id}) or a flat body. D-006 can reuse it for
  session recaps.
- APIClient.recap(from:to:speak:) POSTs {from, to, speak} (epoch seconds); fixture mode returns
  Fixtures/recap_today.json (backend shape, Bryan's voice, about the 3:26-4:09 PM fixture day).
- App/SummaryCard.swift (pure): DailySummary {recap, fetchedAt}; SummaryCard.derive(summary:
  loading:failed:hasEpisodes:locale:timeZone:) -> status ("as of 6:12 PM" / "Updating…" / nil),
  body (.empty / .writing / .failed / .recap), showsRefresh. Tests/SummaryCardTests.swift (11).
- AppState: summary, summaryLoading, summaryFailed, summaryCard; loadSummaryIfNeeded() (once,
  when episodes exist; re-armed if the Home task is cancelled mid-fetch), refreshSummary()
  (midnight -> now, speak false; failures stay on the card, never lastError); stopWatching()
  refreshes the summary when today has episodes.
- HomeView: summary panel after the metrics; header row stacks at accessibility sizes;
  suggestions are Labels with arrow.turn.down.right in muted; Refresh (.glass) bottom right.
  Ledger heading renamed "Today" -> "Log" (the summary owns "Today" now; D-008 makes it a
  DisclosureGroup).
- Demo-only `-scrollTo <metrics|summary|log>[-end]` (AppState.homeScrollTarget, HomeScrollTarget,
  tested in AppScreenTests). Add new HomeSection cases as D-005..D-008 add sections.
- Fixed a D-001 bug found in dark mode: WatchButton label is Brian.page (was white on the
  near-white ink fill). ConnectView's and AddItemView's .glassProminent buttons likely have the
  same problem in dark mode: D-010 should check them.

Screenshots: D-004-home.png (top of card), D-004-home-refresh.png (suggestions + Refresh),
D-004-home-xxl.png, D-004-home-dark.png.

Next iteration must know
- The empty-state sentence is covered by SummaryCardTests; D-009 should add a -empty screenshot.
- Home sections are long: use `-scrollTo <section>` (and `-end`) for screenshots below the fold.

## 2026-09-24 D-005 Protocol card on Home and the reworked Protocol tab (passes)
What I did
- App/ProtocolSummary.swift (pure): derive(items:now:locale:timeZone:) -> rows in window order
  (name, kind, window "7:00–10:00 AM", state, stateSymbol, tone, checked) + "3 of 5".
  States: seen "Seen 8:42 AM" (checkmark.circle.fill, ink), done "Done" (checkmark.circle, ink),
  missed "Missed" (xmark.circle, cost), waiting/undone by the clock: "Later" before the window,
  "Open until 10 PM" inside it, "Missed" once it closed (the backend only marks doses missed).
- App/ProtocolTemplates.swift: six groups (Doses, Meals, Movement, Light, Sleep, Screens), 3-5
  each, from phone/src/content/protocols.ts; Light/Movement -> walk, Sleep/Screens -> winddown;
  windows crossing midnight end at 23:59 (backend needs start < end). isAdded matches by name.
- APIClient.updateProtocol (PUT /api/protocol/{id}, fixture mode keeps status);
  AppState.updateProtocolItem, toggleProtocolItem, protocolSummary(now:), protocolLaunch.
- HomeView: protocol card after the summary (HomeSection.protocol); whole card is a plain Button
  that switches to the Protocol tab (RootView passes openProtocol); empty card "No protocol yet."
  + .glass "Set one up". Home's LazyVStack is now a VStack: -scrollTo landed short at XXL.
- ProtocolView: "3 of 5 today" + Add (stacked at AX sizes), leading 44 pt checkbox, name/window
  is a button that opens AddItemView(editing:), evidence thumbnail only when the image loads,
  swipe actions kept, Templates section of DisclosureGroups with Add / "Added".
- AddItemView: edit mode (pre-filled, "Edit item", Save); kinds are the backend's four (it used
  to offer "sleep", which the backend rejects with 400); Save disabled unless start < end.
- Fixture: Daylight walk is now seen at 3:29 PM (fixture day), so the demo reads 3 of 5.
- Kind symbols are dropped at accessibility sizes on the card and tab rows (they squeezed names).

Screenshots: D-005-home, -home-xxl, -protocol, -protocol-xxl, -protocol-templates, -protocol-edit.
Launch: -scrollTo protocol; -screen protocol-templates; -screen protocol-edit.

Next iteration must know
- Tests with times normalise ICU's U+202F before AM/PM (see plain() in ProtocolSummaryTests).
- The -empty (D-009) card state is "No protocol yet." + Set one up; tab says "Nothing yet today".

## 2026-09-24 D-006 Sessions row and session detail (passes)
What I did
- App/SessionsSummary.swift (pure): derive(sessions:recaps:bodies:dayStart:now:...) -> rows newest
  first (span "3:25–3:39 PM" / "Since 3:53 PM", duration, recapID = newest listing for the
  session, recap body, recapState .recap/.writing/.running), title "Sessions · 3 · 43 min",
  entries(_:in:now:) for the log rows inside a session. Tests/SessionsSummaryTests.swift (13).
- App/Ledger.swift: HomeView's log derivation (entries, outcome, labels, held-back count) moved
  out verbatim so SessionDetailView reuses it. D-008 should coalesce inside Ledger.
- Models: RecapListing {id, sessionId, generatedAt}, RecapList (GET /api/recaps {"recaps": [...]}).
- APIClient: startSession, endSession, sessions(limit:), recaps(limit:), recap(id:). Fixture
  mode reads Fixtures/sessions_today.json and Fixtures/recaps.json (list + bodies in one file).
- AppState: recapListings, recapBodies (fetched once per id), sessionsLoading, refreshSessions()
  (end of refreshToday; failures logged via os.Logger "sessions", never shown), sessionsSummary,
  dayStart(now:). Start/Stop fire POST /api/session/start | /end in a detached Task (live only),
  then refetch sessions.
- Decision: demo fixtures are one recorded day (2026-09-22, 3:25–4:10 PM CDT), so in demo mode
  dayStart is healthspan.day's midnight; live uses the phone's day. HomeMetrics.watchedSeconds
  takes the same dayStart, so the watched line and the Sessions total agree ("43 min").
  The three demo sessions sit inside the fixture window, each with a recap about its own rows.
- HomeView: Sessions panel (DisclosureGroup, title in the label, muted chevron) between the
  protocol card and the Log; rows push SessionDetailView via navigationDestination(item:).
  Empty: "Sessions" + "No sessions yet today."
- SessionDetailView: duration (number) + span, Summary panel (RecapText, or "Summary still
  writing…" + Refresh, or "The summary writes itself when you stop watching." while open),
  then the Log rows inside the session. RecapText is shared with Home's daily summary.

Screenshots: D-006-home-sessions, -home-sessions-xxl, -session, -session-dark.
Launch: -scrollTo sessions (expands, waits 400 ms so it lands at XXL); -screen session.

Next iteration must know
- -screen session is consumed when sessions arrive (onChange), because bootstrap and Home's task
  race and refreshSessions skips while one is in flight.
- The session detail's Log shows duplicate adjacent "Outdoors" rows until D-008 coalesces.
- Untested on hardware: session start/end against the real backend, recap arriving after Stop.

## 2026-09-24 D-007 Today's stats sheet (passes)
What I did
- App/TodayStats.swift: TodayStats.Cell {label, value?, detail?, emptyWord; text = PRD one-line
  form} and TodayStats.derive(episodes:now:locale:timeZone:) -> eight cells in the PRD's order.
  lengthText (rounded minutes, "Under a minute"), sightings ("1 sighting" / "7 sightings").
- Tests/TodayStatsTests.swift: 11 new tests, one per PRD row, every empty word, the fixture day
  (3:29 PM, 3:28–4:08 PM · 40 min, 4:06 PM, 28 min, 7 sightings, people/hydration empty).
- AppState.todayStats(now:); HomeView "Today's stats" between Sessions and Log
  (HomeSection.stats): Grid of two columns so a row's cells share height; one column at
  accessibility sizes. Cell = surface, radius 14, padding 16; value BrianType.number ink, or the
  empty word in body muted; eating window's length as a secondary line; label secondary muted.
- -scrollTo now always waits 400 ms before scrolling (it landed short at XXL for stats too).

Decisions
- The backend emits `meal` (not `food_sighting`); the eating window takes both kinds.
- Hydration skips alcohol_sighting and caffeine_sighting even if their label says "drink".
- Eating window's value is the span; its length is a `detail` line (the one-line PRD form
  "3:28–4:08 PM · 40 min" wrapped badly in a half-width cell). Cell.text keeps the PRD form.
- Time with people sums kind/label containing people/social/conversation (backend emits
  `conversation`; the fixture has none, so the demo shows "Not tracked yet").

Screenshots: D-007-home-stats, -home-stats-xxl, -home-stats-xxl-end, -home-stats-dark.

Next iteration must know
- Episode has no `dominant` field on the phone; nothing needed it yet.
- Tests normalise U+202F and U+2009 (DateIntervalFormatter puts thin spaces round the en dash).
- Orchestrator's header review (compact Stop capsule) is still open; D-010 or whoever touches it.

## 2026-09-24 D-008 Log: collapsed, coalesced ledger (passes)
What I did
- App/LedgerCoalescer.swift (pure): coalesce(entries) on newest-first rows. A row joins the
  group above when labels match (trimmed, case-insensitive) and it is <= 10 min older than the
  group's earliest row so far (so a steady stream chains). Merged row: earliest row's id and
  time, label of the earliest; outcome + decision + kind + reported from the strongest outcome
  (said > asked > acted > held back), else from any row with a decision (so it still opens).
- Tests/LedgerCoalescerTests.swift: 9 tests incl. the demo fixture (the two "outdoors" rows
  nine seconds apart become one, "decision-d_3156"; no adjacent duplicate labels after).
- HomeView: Log is a DisclosureGroup (title "Log", muted chevron, like Sessions but on the
  page, not a panel), collapsed by default, rows coalesced, "Held back N today" footer inside.
  The whole section is hidden when there are no rows (what D-009 asks for).
- SessionDetailView: its Log coalesces after the session filter (filter first, so a merged
  row's earliest time never falls outside the session).
- -scrollTo log expands the Log; -scrollTo log-end leaves it collapsed at the page's foot.
  APP_NATIVE_NOTES.md launch-argument table updated.

Screenshots: D-008-home-log, -home-log-collapsed, -home-log-dark, -home-log-xxl.

Next iteration must know
- Ledger.heldBackCount still counts raw decisions, not coalesced rows (the footer is a count
  of decisions held back, which merging should not hide).
- Orchestrator's header review (compact Stop capsule) is still open; the full-width Stop bar
  is visible in every D-008 screenshot. D-010 or whoever touches the header.
