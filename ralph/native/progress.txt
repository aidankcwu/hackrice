# Ralph progress (app/native)
Started: Thu Sep 24 14:52:26 CDT 2026
---

## 2026-09-24 · N-001 passed (connection state model)
- New ios/Brian/Sources/App/ConnectionStatus.swift: ConnectionLevel (grey/amber/green/red),
  ConnectionStatus(level,text), StreamStats, ConnectionInputs, and the pure
  ConnectionStatus.derive. Priority: token refused ("Invalid invite link") > link not set
  ("Not connected") > unreachable ("Server unreachable") > glasses unavailable/notRegistered
  ("Glasses off") > not watching (grey "Not watching") > socket down (amber "Reconnecting…")
  > no frame yet (amber "Starting…") > no frame for >10 s (amber "No frames for N s")
  > green "Watching · N min".
- AppState: `connectionStatus` / `connectionStatus(now:)`, `streamStats` / `streamStats(now:)`.
  Views showing minutes or "last frame N s ago" must wrap in TimelineView(.periodic(from:.now, by:1))
  and call the `(now:)` variants. framesSent is per session (baseline taken in startWatching;
  sender.sentCount is cumulative). serverAcknowledged = socket up && >=1 frame delivered this
  session (the server sends no per-frame ack). Token refusal via API shows as
  LinkState.unreachable(APIError.tokenRejected.sentence); `LinkState.isTokenRejected` detects it.
- Demo: AppState.demoStats fakes 1 frame / 1.5 s, ack true, AppState.demoSpokenLine
  ("Coffee this late may cost you sleep tonight.", from the decisions fixture).
- Link files, additive only (header comments say so): MacLink.lastSpokenText/lastSpokenAt set in
  handle() for speak/audio; LinkGlue read-only framesSent/lastFrameAt/lastSpokenText/lastSpokenAt.
- Tests: Tests/ConnectionStatusTests.swift; 43 tests pass on iPhone sim 6C6A1567-….
- NOTE: docs/APP_PRD.md is NOT on app/native; read it with `git show e8483bd:docs/APP_PRD.md` (on main).
- NOTE: this iteration's `git push` was refused by the tool permission layer; commits are local.
  Next iteration: try `git push -u origin app/native` first.
- Next: N-002 needs a demo flag that forces glasses off (e.g. `-glassesOff`) for the red screenshot;
  set `glasses = .unavailable` after the demo branch in AppState.init.

## 2026-09-24 · N-002 passed (status pill, -screen)
- Sources/Screens/StatusPill.swift: StatusPill (dot + ConnectionStatus text, TimelineView 1 s,
  44 pt, VoiceOver label = status text, hint "Opens Connect") and StatusPillToolbar
  (ToolbarItem .topBarLeading). RootView adds it to every tab's toolbar next to the gear;
  the bar's own Liquid Glass capsule is the pill background. A safeAreaBar was tried first
  and blurred Protocol's large title under the scroll-edge effect; don't go back to it.
  Web tabs (N-004) just add `StatusPillToolbar()` to their toolbar.
- Tap calls appState.requestSetup() -> RootView shows SetupView. N-003: point that sheet at
  ConnectView (and `-screen connect`, currently = SetupView sheet).
- Theme.swift helpers: Brian.pending (amber, status only) and ConnectionLevel.color.
- StatusStrip.swift deleted. Today shows appState.lastError as one Brian.cost line + Try again
  (connection problems live in the pill only).
- RootView: AppScreen enum + `-screen <id>` (today, calendar, analysis, protocol, connect,
  settings). calendar/analysis land on Today until N-004 adds tabs: extend AppTab and
  AppScreen.tab then. connect = Setup sheet, settings = Settings sheet. -screen also skips the
  first-launch Setup sheet. Tests/AppScreenTests.swift (46 tests pass).
- AppState: `-glassesOff` (demo only) sets glasses = .unavailable -> red "Glasses off" pill.
- Screenshots: first launch after `simctl install` needs ~5 s, not 3 (3 s gave a blank PNG).
- Push: `git push` is refused by the tool permission layer again; commits are local only.

## 2026-09-24 · N-003 passed (Connect screen)
- Sources/App/ConnectRows.swift: pure ConnectRows.derive(ConnectionInputs, checking:, registering:)
  -> three ConnectRow {level, text, fix?}. Invite: Checking… / Invalid invite link (fix =
  APIError.tokenRejected.sentence) / Paste the link from your invite / Server unreachable (fix =
  the LinkState sentence) / Server reachable · label. Glasses: Registering… / Glasses off (red,
  ConnectRows.glassesOffFix) / Open Meta AI and tap Allow / Connecting… (registered+watching) /
  Registered / Connected. Stream: same order as the pill; green text uses NO-BREAK spaces
  (\u{00A0}) between numbers and units so wraps never split "1 s". Also ServerURL.isInviteLink.
  Tests/ConnectRowsTests.swift (55 tests pass).
- AppState additions: connectRequested + requestConnect() (renamed from setupRequested /
  requestSetup), checkingLink (set in testServer), registeringGlasses, clipboardOffer,
  checkClipboard() (UIPasteboard.detectedPatterns probableWebURL: no paste prompt),
  useClipboardLink() (reads on tap, applies only if isInviteLink), connectRows(now:).
  Demo flags: `-fresh` (no link, glasses notRegistered, not watching, clipboard offer shown),
  `-tokenRejected` (red invite row, not watching). Red screenshot = -tokenRejected -glassesOff.
- Sources/Screens/ConnectView.swift: List(.insetGrouped) in a TimelineView (1 s). Rows section,
  primary button section (clear background, .glassProminent), lastError line only if not a row
  fix, "Live" section while watching (Frames sent, Server acknowledged, Bryan last said, Test
  voice), DisclosureGroup "Permissions". `.tint(Brian.ink)` is REQUIRED on any presented view:
  sheets/covers do not inherit RootView's tint (the button went system blue without it).
- RootView: fullScreenCover on first launch (non-demo, key "setupSeen" kept), sheet from the pill
  / Settings; `-screen connect` and legacy `-showSetup` open the full-screen cover.
- SetupView.swift deleted; PermissionRow moved to Screens/PermissionRow.swift. Settings' "Open
  Setup" is now "Connect" (N-005 does the rest of Settings). APIError.notConfigured now says
  "Paste the link from your invite in Connect."
- Screenshots N-003-connect-{empty,ready,red}.png.
- Push: refused by the tool permission layer again; commits are local only.

## 2026-09-24 · N-004 passed (Calendar and Analysis web tabs)
- ServerURL.webBase: https://DOMAIN[:port]/t/NAME/app from a wss:// link; nil for ws:// (LAN).
- Sources/App/WebSource.swift: pure WebSource.derive(demo:, override:, server:) -> .page(base,
  token) / .noLink / .lan / .seeded; WebSource.url(base:path:token:) -> <base><path>?token=T&embed=1
  (embed=1 on every load, token only when present); WebSource.override reads `-webBase <url>`
  (wins) or BRIAN_WEB_BASE, http(s) only, trailing slash trimmed; ignored outside demo mode.
  Tests/WebSourceTests.swift (68 tests pass in total).
- AppState.webSource (computed). `server` is no longer @ObservationIgnored, so the web tabs follow
  a changed invite link; webBaseOverride is read in the demo branch of init.
- Sources/Screens/WebScreen.swift: WebScreen(title:path:) + WebLoad (@Observable view state:
  loading/loaded/failed/rejected, attempt counter) + WebPageView (UIViewRepresentable WKWebView,
  default data store shared by both tabs). Sets a non-HttpOnly cookie zeroist_embed=1 for the host
  before the load. 401/403 main-frame -> rejected (APIError.tokenRejected.sentence + Open Connect);
  5xx or a failed load -> "<Title> did not load. Check that the phone is online, then try again."
  + Try again. Links to another host open in Safari. Spinner overlay only until the first didFinish.
  Page state has no native title (the web app shows its own); message states use a large title.
  `.onChange(of: appState.webSource)` retries, so a new link clears a failed/rejected state.
- RootView: AppTab is CaseIterable, order today, calendar, analysis, protocol; symbols calendar
  and chart.bar. AppScreen.tab maps calendar/analysis to their tabs. Each web tab has the pill +
  gear toolbar like the others.
- Screenshots: N-004-calendar.png, N-004-analysis.png (-demo -screen <tab> -webBase
  http://127.0.0.1:3100, wait 12 s), N-004-calendar-seeded.png (no -webBase).
- Fixtures server: this branch's phone/ has NO embed support; export app/web's instead:
  from repo root `git archive --format=tar -o $SCRATCH/webphone.tar origin/app/web phone`
  (git archive from ios/Brian yields an empty tar: it scopes to the cwd), extract, symlink
  node_modules to /Users/ljawhomebot/hackrice/phone/node_modules, then
  `TURBOPACK_ROOT=/ NEXT_PUBLIC_FIXTURES=1 npx next dev -p 3100`. Next 16 dev blocks its own
  scripts to the 127.0.0.1 origin (page sticks on "Loading the month…"): add "127.0.0.1" to
  allowedDevOrigins in the SCRATCH copy of next.config.ts (never in phone/). Worth telling the
  web loop to add it upstream.
- Web-side gap seen in N-004-analysis.png (known, in APP_WEB_NOTES): week chart clipped left.
- Push: refused by the tool permission layer again; commits are local only.

## 2026-09-24 · N-005 passed (Settings cleanup)
- Merged origin/main first (Bryan spelling pass, Config.xcconfig/Local.xcconfig.example,
  PrivacyInfo). Only conflict was Brian.xcodeproj/project.pbxproj: took theirs and re-ran
  `xcodegen generate`; always resolve pbxproj that way (it is generated from project.yml).
- SettingsView rewritten: "Invite link" section (token-free label from link state, "Change" or
  "Paste link"; unreachable adds the sentence in Brian.cost), a Connect row showing the live
  ConnectionStatus dot + text, Voice ("Speak through the glasses" + footer), collapsed
  Permissions DisclosureGroup (keeps the whole list on one screen), Record corpus toggle in
  every build (was DEBUG-only; off by default via defaults.bool), DEBUG section (status line,
  mock glasses; "Say a test line" dropped since Connect has Test voice), About: Version, Build.
  `.tint(Brian.ink)` on the sheet.
- Settings has NO server field any more. AppState.requestLinkChange() sets linkChangeRequested
  + connectRequested; ConnectView's .task opens its paste field when the flag is set and clears it.
- Screenshot N-005-settings.png (-demo -screen settings).
- Next: N-006 design pass + docs/APP_NATIVE_NOTES.md. Dynamic Type XXL screenshots: try
  `xcrun simctl ui $DEV content_size accessibility-extra-extra-large` before launching, and reset
  with `content_size large` afterwards.
